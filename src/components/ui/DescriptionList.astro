---
import { cn } from '../../utils/cn';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<dl
  class={cn(
    "grid grid-cols-1 text-base/6 sm:grid-cols-[min(50%,theme(spacing.80))_auto]",
    className,
  )}
>
  <slot />
</dl>
