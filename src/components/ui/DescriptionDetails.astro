---
import { cn } from '../../utils/cn';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<dd
  class={cn(
    "font-medium pb-3 pt-1 text-zinc-950 sm:border-0 sm:border-zinc-950/5 sm:py-3 dark:text-white dark:sm:border-white/5 sm:[&:nth-child(2)]:border-none",
    className,
  )}
>
  <slot />
</dd>
